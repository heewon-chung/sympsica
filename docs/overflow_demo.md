# Overflow-detection demo (W6.3)

**This is a PLAINTEXT/REFERENCE-ONLY demonstration.** No MPC overflow detector exists anywhere in this codebase; `src/protocols/salt.cpp`'s `OverflowChecker::check` is a `[[noreturn]]` stub that always aborts. The real protocol's T=4 no-overflow assumption (`Params::T`) is checked HERE, in the clear, against `ref/reference.py`'s `Ref.detect_overflow` -- never against a running MPC circuit. This report must never be read as evidence of an implemented MPC overflow detector.

## Setup

5 ids are planted to collide in one synthetic bucket (777): `[10001, 10002, 10003, 10004, 10005]`. Every other id maps to itself (no incidental collisions). This construction is `ref/reference.py`'s `Ref.build_overflow_demo_data()`, the SAME planted case `_selftest_overflow()` already proves at import time (task-18-brief.md SC5/FC3) -- committed here as a named fixture (`test/fixtures/overflow_planted.fixture`) instead of only an inline self-test.

## Result: deep scan (T' = 7)

- Depth-7 syndrome vector `d_1..d_13` (2*T'-1 = 13 terms): `[646364557242356487, 1937558631415186103, 780889260909932386, 997745954457173898, 670750315835778520, 412896386929052568, 741051222477068828, 517789724850478991, 809741256326956965, 922114123835497334, 582166299015236210, 2121001166991829848, 142251759868216111]`

- Recovered rank: **5**

- Flagged overflowing: **YES** (T_CAP = Params::T = 4)

## Result: no deep headroom (T' = 4)

- Recovered rank: **4** (a depth-(2*4-1)=7 Hankel test cannot report a rank above 4 -- structural, not incidental)

- Flagged overflowing: **NO**

This is the demo's whole point: the true multiplicity (5) is invisible to a scan with no headroom beyond the real protocol's own depth; only the deep T'=7 scan sees past it.

## Result: after salt refresh

- Overflowing buckets found: **NONE**

- Planted bucket (777) still present in the scan: **NO** (the 5 planted ids disperse to 5 distinct buckets under the refreshed oracle, so bucket 777 is no longer touched at all)


---

Generated by `test/overflow/gen_overflow_demo.py` from `test/fixtures/overflow_planted.fixture` (generator invocation: `python3 ref/reference.py emit-ovf --out test/fixtures/overflow_planted.fixture`).
